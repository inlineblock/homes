# Deep iron red brick | running bond | v003

![Native preview](preview.png)

Use Brick meters UV; call library.build_lindon_assets.apply_brick_uv(obj) after final mesh and world transforms. Module bottom-center, front -Y, Z-up.

Fixed physical course scale, half-running bond; subtle internal per-unit red variation. No arbitrary object scaling of the physical module.

Original concept appearance, not a manufacturer product or weather/structural certification. Host integration is reviewed separately.

Original by Homes project contributors, CC BY 4.0. Regenerate and verify with `tools/library/build_lindon_palette.py` (MIT). Existing adopted parent versions are immutable. Read `asset.json` for exact pinned dependencies, dimensions and operating assumptions.

Derived from warm-red-brick v002, which remains available as the lighter russet option. Generate only this version with `-- deep-publish`; preview with `-- deep-previews`; verify with `-- deep-verify`.

Inspected actual 1100 x 950 neutral CPU preview: deeper iron-red / subdued burgundy unit colors with fine warm-gray joints. It remains visibly red rather than flat brown; less terracotta/orange than v002. Physical bond and restrained texture are unchanged. Host sun/shade review remains with the adopting project.
