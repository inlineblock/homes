# New home

Describe the brief, area convention, design priorities, and current status here.

Put editable models in `model/`, drawings in `outputs/plans/`, selected renders in `outputs/images/`, interchange files in `exports/`, reference notes in `references/`, and home-specific assets in `assets/`.

[Current images and plans](outputs/README.md). Embed the primary image above this link once it exists. Keep current outputs only, using stable filenames; follow `homes/AGENTS.md`.

Start with `program.md`, the [program checklist](../../docs/home-program-checklist.md) and [design handbook](../../docs/design-guide/README.md). Create `assets/README.md` for actual linked dependencies and `design-review.md` for measured checks and unresolved decisions. Keep assumptions separate from verified facts.
