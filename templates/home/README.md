# New home

Describe the brief, area convention, design priorities, and current status here.

Put editable models in `model/`, drawings in `outputs/plans/`, selected renders in `outputs/images/`, interchange files in `exports/`, reference notes in `references/`, and home-specific assets in `assets/`.

[Current images and plans](outputs/README.md). Keep current outputs only, using stable filenames; follow `homes/AGENTS.md`. Embed the complete reviewed gallery here, not just a hero image or a link.

## Photographic tour

Lead this page with the reviewed exterior Image Gen study, labeled as an AI photographic study with a provenance link. Follow the create-home and output QA skills for a generous multi-angle exterior/interior tour after native render review. Register the actual hero and additional studies in gallery.json; pair interior studies with their native sources. Keep all native coverage below.

## Exterior

Embed three distinct native views: front/arrival, rear/outdoor living, and side or elevated roof/site context. Caption what each explains.

## Interiors

Embed at least two complementary interior views, including the kitchen and another useful room or view of the main living space.

## Signature features

Give each defining feature its own clearly framed image and caption; show paired states for moving glazing or equipment where useful. A view above can fulfill this when the feature is its subject.

## Floor plans

Embed a readable furnished plan for every occupied level, beyond the minimum five renders. Link available PDF and vector originals.

Record the reviewed paths and coverage in `gallery.json`, mirror all current images in `outputs/README.md`, add a complete compact gallery to the root README, and run `python3 tools/common/verify_galleries.py` from the repository root. Remove these scaffold instructions as real content is added; never publish broken image placeholders.

Start with the create-home skill and `design-intent.md` before modeling, then `program.md`, the [program checklist](../../docs/home-program-checklist.md) and [design handbook](../../docs/design-guide/README.md). Create `assets/README.md` for actual linked dependencies and `design-review.md` for measured checks and unresolved decisions. Keep assumptions separate from verified facts.
