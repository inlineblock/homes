# Slim dark three-panel window — 2400 ×2400 mm

![Actual native preview](preview.png)

Origin bottom-center at exterior main frame plane Y0, front -Y, Z-up. Main frame occupies Y0..0.14; sill projects to Y-0.017; interior pull reaches Y0.17575. Host opening must remain clear across the full 2.4 × 2.4 m envelope.

Keep scale 1 for 55 mm sightlines; publish dimensional variants using generator for alternate openings. Moderate host scaling is only a visual study and changes all product dimensions.

Generic original concept, not commercial product. Side panels are modeled closed; no operational or weather certification. Reused cabinet pulls are concept grips only, not specified casement-lock hardware.

Original by Homes project contributors; CC BY 4.0. Editable generator: `tools/library/build_lindon_assets.py` (MIT). Use `link_assets(ROOT)` to link the exact material/collections. Inspect `asset.json` for dimensions and dependencies.

Preview: Blender Cycles CPU, 48 samples, AgX Medium High Contrast, exposure 0, neutral studio world 0.45 and large area lights. Actual sunlit/shaded host validation remains the adopting home’s responsibility.
