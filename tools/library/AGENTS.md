# Library authoring

- Publishing creates a new version folder; never overwrite an already published library file.
- When promoting an asset from a home, preserve its material and geometric identity, normalize its origin, and record provenance.
- Include `asset.json` and native source. Verify that linked instances in another home resolve relative to the repository.
