# New home — current outputs

[Project and editable models](../README.md)

Lead with the reviewed exterior Image Gen photographic study and its provenance link. Embed the selected photographic tour, including interiors paired with their native sources; follow the output QA skill for coverage and source checks.

Embed every selected current render with a useful caption and full-image link: at least three exterior angles and two interiors, plus dedicated views for each signature feature. Embed readable plans for every occupied level in addition to the renders; link each PDF and SVG. Mirror this full gallery in the home README and add complete minimum coverage to the root catalog, following `homes/AGENTS.md`. Keep `gallery.json` aligned and run the gallery verifier.

Keep one current set. Replace the same filenames after review; Git retains prior revisions. Keep drafts in ignored `work/`.
