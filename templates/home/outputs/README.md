# New home — current outputs

[Project and editable models](../README.md)

Add labeled image previews linking to `images/<view>.png`, and links to each PDF, SVG, and preview in `plans/` as deliverables are reviewed. Embed the primary render in the home README and add a preview to the root catalog.

Keep one current set. Replace the same filenames after review; Git retains prior revisions. Keep drafts in ignored `work/`.
