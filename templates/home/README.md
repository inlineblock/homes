# New home

Describe the brief, area convention, design priorities, and current status here.

Put editable models in `model/`, drawings in `drawings/`, selected renders in `renders/`, interchange files in `exports/`, reference notes in `references/`, and home-specific assets in `assets/`.
